# Personal Skills

This is a repository of AI skills based on my personal coding habits.

## Installation

If on WSL, install dependencies to run `cmd.exe` scripts:

```shell
sudo apt install binfmt-support
```

Install with the `./install.sh` script.
